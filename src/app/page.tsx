// This route is handled by src/app/(protected)/page.tsx to avoid routing conflicts.
// The middleware will redirect unauthenticated users to /login.
export default function RootPage() {
  return null
}
